import styled from "styled-components";
import theme from "../global";

export const Video = styled.iframe`
  border: 4px solid ${theme.colors.courseFrontEnd};
  border-radius: 4px;
  margin-left: 42px;
`;