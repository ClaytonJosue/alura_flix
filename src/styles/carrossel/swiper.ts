import styled from "styled-components";

export const SwiperBox = styled.section`
  .swiper-container {
    position: relative;
    top: -200px;

    .swiper-slide {
      margin-right: 25px;
    } 
  }
`;